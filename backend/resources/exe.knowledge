@EXE.DECISION = {};
@EXE.EFFECT = {};
@EXE.EFFECTOR = {};
@EXE.CAPABILITY = {};
@EXE.CALLBACK = {};
@EXE.INPUT-TMR = {};
@EXE.TRANSIENT-FRAME = {};

@EXE.PHYSICAL-EFFECTOR = {
    ISA @EXE.EFFECTOR
};

@EXE.VERBAL-EFFECTOR = {
    ISA @EXE.EFFECTOR
};

@EXE.MENTAL-EFFECTOR = {
    ISA @EXE.EFFECTOR
};

@EXE.STATEMENT = {};

@EXE.RETURNING-STATEMENT = {
    ISA @EXE.STATEMENT;
};

@EXE.BOOLEAN-STATEMENT = {
    ISA @EXE.RETURNING-STATEMENT;
};

@EXE.ADDFILLER-STATEMENT = {
    ISA @EXE.STATEMENT;
    CLASSMAP "AddFillerStatement";
};

@EXE.ASSERT-STATEMENT = {
    ISA @EXE.STATEMENT;
    CLASSMAP "AssertStatement";
};

@EXE.ASSIGNFILLER-STATEMENT = {
    ISA @EXE.STATEMENT;
    CLASSMAP "AssignFillerStatement";
};

@EXE.ASSIGNVARIABLE-STATEMENT = {
    ISA @EXE.STATEMENT;
    CLASSMAP "AssignVariableStatement";
};

@EXE.EXISTS-STATEMENT = {
    ISA @EXE.BOOLEAN-STATEMENT;
    CLASSMAP "ExistsStatement";
};

@EXE.EXPECTATION-STATEMENT = {
    ISA @EXE.STATEMENT;
    CLASSMAP "ExpectationStatement";
};

@EXE.FOREACH-STATEMENT = {
    ISA @EXE.STATEMENT;
    CLASSMAP "ForEachStatement";
};

@EXE.IS-STATEMENT = {
    ISA @EXE.BOOLEAN-STATEMENT;
    CLASSMAP "IsStatement";
};

@EXE.MAKEINSTANCE-STATEMENT = {
    ISA @EXE.RETURNING-STATEMENT;
    CLASSMAP "MakeInstanceStatement";
};

@EXE.MP-STATEMENT = {
    ISA @EXE.RETURNING-STATEMENT;
    CLASSMAP "MeaningProcedureStatement";
};

@EXE.OUTPUTXMR-STATEMENT = {
    ISA @EXE.RETURNING-STATEMENT;
    CLASSMAP "OutputXMRStatement";
};

@EXE.TRANSIENTFRAME-STATEMENT = {
    ISA @EXE.RETURNING-STATEMENT;
    CLASSMAP "TransientFrameStatement";
};

@EXE.XMR = {};

@EXE.AMR = {
    ISA @EXE.XMR;
};

@EXE.MMR = {
    ISA @EXE.XMR;
};

@EXE.TMR = {
    ISA @EXE.XMR;
};

@EXE.VMR = {
    ISA @EXE.XMR;
};